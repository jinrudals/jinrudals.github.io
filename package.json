{
  "name": "jinrudals.github.io",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "npm run generate:content && vite --host 0.0.0.0",
    "generate:content": "node scripts/generate-content.mjs",
    "build": "npm run generate:content && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "npm run generate:content && svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "test": "npm run generate:content && node --test"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.8",
    "@sveltejs/kit": "^2.22.0",
    "@sveltejs/vite-plugin-svelte": "^5.1.0",
    "svelte": "^5.34.0",
    "svelte-check": "^4.2.2",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
